# 截图资源

主 README 引用这个目录里的图片。当前状态:

| 文件名 | 内容 | 状态 |
| --- | --- | --- |
| `home.png` | 首页概览(顶部总览 + 全部服务器卡片墙) | ✅ 已就位(README 顶部 banner) |
| `detail.png` | 服务器详情:实时指标 + 负载曲线 | ✅ 已就位 |
| `probe.png` | 延迟探测:ICMP/TCP/HTTP 曲线 | ✅ 已就位 |

想替换某张:重新截图,按上表文件名覆盖即可。

提示:
- 宽度 1600px 以上在 GitHub 上最清晰(现有两张是 2560×1440,很棒);深色主题更出片。
- 想展示「网速数字逐位滚动」动效,可录一小段 GIF 命名 `home.gif`,在 README 里把 `home.png` 换成它。
- 单张尽量压到 < 500KB(tinypng / squoosh),避免仓库变大、加载变慢。
